# chrono-pi — docs

- **[`REBUILD.md`](REBUILD.md)** — the governing order: rebuild direction, product contract, the
  mathematical core, named results, workstreams, phases, MVP. Start here.
- **[`reference-table.md`](reference-table.md)** — the v1 reference readings, locked by the engine
  regression tests (`packages/engine/test/reference.test.ts`). A test fixture — do not move it.
- **[`v1-archive/`](v1-archive/)** — the original v1 design package (`00`–`07`, `conventions.md`, the
  phase specs, `IMPORT.md`). **Superseded reference**: it documents what v1 built (the scan-based
  engine), not the order to execute. Kept for history and because v1 is the rebuild's regression oracle.
